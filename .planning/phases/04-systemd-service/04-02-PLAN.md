---
phase: 04-systemd-service
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - SRV-02
  - SRV-03

must_haves:
  truths:
    - "After a VM reboot, systemctl status shows the service came up automatically"
    - "After killing the main Gunicorn PID, systemd restarts it within RestartSec=5 seconds"
    - "curl from an agent machine (not the VM) returns HTTP 200 from /health while under systemd management"
  artifacts:
    - path: "/etc/systemd/system/context-of-code.service"
      provides: "Enabled systemd unit file — already created in plan 01"
      contains: "WantedBy=multi-user.target"
  key_links:
    - from: "systemd"
      to: "context-of-code.service"
      via: "WantedBy=multi-user.target + systemctl enable"
      pattern: "WantedBy=multi-user.target"
    - from: "systemd"
      to: "gunicorn process"
      via: "Restart=on-failure after SIGKILL"
      pattern: "Restart=on-failure"
---

<objective>
Verify boot persistence and crash recovery: reboot the VM, confirm the service autostarted, kill the Gunicorn process to confirm systemd restarts it, and confirm the /health endpoint is reachable from an agent machine.

Purpose: Completes the Phase 4 proof — the roadmap success criteria require reboot verification, crash recovery verification, and external reachability while under systemd management. These cannot be automated without direct human interaction on the VM.

Output: Phase 4 complete — aggregator confirmed running as a managed systemd service.
</objective>

<execution_context>
@/Users/tjcla/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjcla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-systemd-service/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify boot persistence (reboot the VM)</name>
  <files>N/A — verification only, no files modified</files>
  <what-built>
Plan 01 installed the systemd unit file at `/etc/systemd/system/context-of-code.service`, ran `systemctl enable --now context-of-code`, and configured UFW. The service is currently running. Now we verify it survives a full reboot.
  </what-built>
  <action>
Human verification required. Reboot the VM and confirm the service autostarted. Follow the steps in how-to-verify.
  </action>
  <how-to-verify>
**Step 1 — Reboot the VM:**
```bash
sudo reboot
```
Wait ~60 seconds for the VM to come back online.

**Step 2 — Reconnect via SSH on port 2214:**
```bash
ssh -p 2214 student@<VM_IP>
```
If SSH fails immediately, wait another 30 seconds and retry (the VM may still be booting).

**Step 3 — Check service autostarted:**
```bash
sudo systemctl status context-of-code --no-pager
```

Expected output:
- `Active: active (running)` — service started without manual intervention
- `Loaded: loaded (/etc/systemd/system/context-of-code.service; enabled; ...)`
- A recent timestamp (matching the reboot time)

**Step 4 — Confirm UFW is still active:**
```bash
sudo ufw status
```
Expected: `Status: active` with 5000/tcp and 2214/tcp ALLOW rules still in place.

**Roadmap Success Criterion 2:** "After a VM reboot, `systemctl status context-of-code` shows the service came up automatically without manual intervention."
  </how-to-verify>
  <verify>
    <automated>MISSING — requires human action (VM reboot); automated reboot verification is not possible via CLI from outside the VM without additional tooling</automated>
    <manual>After rebooting and reconnecting via SSH: `sudo systemctl status context-of-code --no-pager` must show `Active: active (running)` and `enabled`</manual>
  </verify>
  <done>
After reboot, `systemctl status context-of-code` shows `Active: active (running)` without any manual start. UFW is still active with rules in place. Roadmap success criterion 2 is satisfied.
  </done>
  <resume-signal>
Type "boot-ok" if the service autostarted correctly after reboot, or describe what you see (e.g., the actual systemctl status output) if something is wrong.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify crash recovery and external reachability</name>
  <files>N/A — verification only, no files modified</files>
  <what-built>
The service is confirmed running after reboot (Task 1). Now verify that systemd restarts Gunicorn after a crash, and confirm the /health endpoint is reachable from an agent machine while under systemd management.
  </what-built>
  <action>
Human verification required. Kill the Gunicorn process and confirm systemd restarts it. Then curl /health from an agent machine. Follow the steps in how-to-verify.
  </action>
  <how-to-verify>
**Crash recovery test:**

Step 1 — Find the main Gunicorn PID:
```bash
systemctl status context-of-code | grep "Main PID"
```
Note the PID number (e.g., `Main PID: 1234 (gunicorn)`).

Step 2 — Kill the process:
```bash
sudo kill <PID>
```

Step 3 — Wait 5-10 seconds (RestartSec=5 in the unit file), then check:
```bash
sudo systemctl status context-of-code --no-pager
```

Expected: `Active: active (running)` with a NEW PID and a restart count visible (e.g., `Main PID: 5678 (gunicorn)`).

**Roadmap Success Criterion 3:** "After killing the Gunicorn process, systemd restarts it within the configured restart delay."

---

**External reachability test (from an agent machine, not the VM):**

From a separate machine (laptop, agent device — NOT from the VM itself):
```bash
curl http://<VM_IP>:5000/health
```

Expected response:
- HTTP 200
- Body: `{"status": "ok", "ts": "2026-..."}` (valid ISO 8601 UTC timestamp)

**Roadmap Success Criterion 4:** "`curl http://<VM_IP>:5000/health` from an agent machine succeeds while the service is running under systemd."

---

Report both results: crash recovery outcome AND curl output from the agent machine.
  </how-to-verify>
  <verify>
    <automated>
From the VM (SSH session): `sudo systemctl status context-of-code --no-pager` after killing the PID — must show `Active: active (running)` with a new PID.

From agent machine: `curl -s -o /dev/null -w "%{http_code}" http://<VM_IP>:5000/health` — must return `200`.
    </automated>
    <manual>Crash recovery: kill the main Gunicorn PID, wait 5-10 seconds, confirm new PID appears in systemctl status. External reach: run curl from a non-VM machine and confirm HTTP 200 with JSON body.</manual>
  </verify>
  <done>
Systemd restarts the Gunicorn process after it is killed (new PID visible within ~5 seconds). `curl http://<VM_IP>:5000/health` from an agent machine returns HTTP 200 with `{"status": "ok", "ts": "..."}`. All Phase 4 roadmap success criteria satisfied.
  </done>
  <resume-signal>
Type "verified" if both crash recovery and external curl succeeded. Or describe any issues for diagnosis.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when all five roadmap success criteria are satisfied:

1. `systemctl status context-of-code` shows `Active: active (running)` — confirmed in Plan 01
2. After VM reboot, service autostarted — confirmed in Task 1 above
3. After killing the Gunicorn PID, systemd restarted it — confirmed in Task 2 above
4. `curl http://<VM_IP>:5000/health` from agent machine returns HTTP 200 — confirmed in Task 2 above
5. `sudo ufw status` lists port 5000/tcp ALLOW — confirmed in Plan 01 Task 2

All five criteria satisfied = Phase 4 complete.
</verification>

<success_criteria>
- VM reboots and `systemctl status context-of-code` shows `Active: active (running)` without any manual intervention
- Killing the main Gunicorn PID results in systemd restarting the service within ~5 seconds
- `curl http://<VM_IP>:5000/health` from an agent machine returns `{"status": "ok", "ts": "..."}` HTTP 200 while the service is under systemd management
- All five Phase 4 roadmap success criteria are met
</success_criteria>

<output>
After completion, create `.planning/phases/04-systemd-service/04-02-SUMMARY.md` following the summary template.

Then update STATE.md: Phase 4 complete, all requirements SRV-02 and SRV-03 satisfied.
</output>

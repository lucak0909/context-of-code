---
phase: 03-health-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["web_app/app.py"]
autonomous: false
requirements: ["HEALTH-01"]
must_haves:
  truths:
    - "User can query the health endpoint from outside the VM"
    - "Health endpoint returns HTTP 200 with an 'ok' status"
    - "Health endpoint returns a valid UTC ISO 8601 timestamp"
  artifacts:
    - path: "web_app/app.py"
      provides: "The /health HTTP route"
      contains: "@app.route('/health'"
  key_links:
    - from: "web_app/app.py"
      to: "flask.jsonify"
      via: "HTTP response generation"
      pattern: "jsonify"
---

<objective>
Implement the `/health` liveness probe endpoint on the Flask application and verify it's reachable externally.

Purpose: Provide external agents and monitoring systems a reliable way to verify the service is running.
Output: `GET /health` route returning a valid JSON status and UTC timestamp.
</objective>

<execution_context>
@/Users/tjcla/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/tjcla/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-health-endpoint/03-RESEARCH.md
@web_app/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Health Endpoint</name>
  <files>web_app/app.py</files>
  <action>
    Import `datetime`, `timezone`, and `jsonify`. Add a new route `@app.route('/health', methods=['GET'])` directly to the `app` instance in `web_app/app.py`.
    It must return a JSON response containing `{"status": "ok", "ts": "<utc-iso-string>"}` using `datetime.now(timezone.utc).isoformat()`.
    Ensure it returns an HTTP 200 status code. Do NOT add this route to the `api_bp` blueprint, it must be at the root `/health`.
  </action>
  <verify>
    <automated>python -c "from web_app.app import app; client = app.test_client(); resp = client.get('/health'); assert resp.status_code == 200; assert resp.get_json()['status'] == 'ok'; assert 'ts' in resp.get_json(); print('OK')"</automated>
  </verify>
  <done>GET /health returns HTTP 200 with the correct JSON payload structure via the test client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify External Reachability</name>
  <files>web_app/app.py</files>
  <action>Ask the user to manually verify the endpoint from an external machine.</action>
  <verify>
    <manual>
    Run gunicorn and curl from another machine to ensure port 5000 receives traffic and returns HTTP 200.
    </manual>
  </verify>
  <done>External HTTP request to /health returns a successful response.</done>
  <what-built>The `/health` liveness probe endpoint on the aggregator.</what-built>
  <how-to-verify>
    1. Start gunicorn from the project root: `venv/bin/gunicorn --bind 0.0.0.0:5000 web_app.app:app`
    2. From a separate machine (e.g. your local laptop, not the VM), run: `curl http://<VM_IP>:5000/health`
    3. Verify the output is similar to: `{"status": "ok", "ts": "2026-02-24T12:00:00.000000+00:00"}`
    4. Stop the gunicorn server with Ctrl+C.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- The route is `/health` (not `/api/health`).
- The timestamp is timezone-aware and compliant with ISO 8601 UTC.
- External reachability verified manually.
</verification>

<success_criteria>
- `curl http://<VM_IP>:5000/health` from an external machine returns HTTP 200 and expected payload.
</success_criteria>

<output>
After completion, create `.planning/phases/03-health-endpoint/03-01-SUMMARY.md`
</output>

---
phase: 05-agent-cutover-and-end-to-end-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /etc/systemd/system/context-of-code.service  # on VM
autonomous: false
requirements:
  - SRV-03

must_haves:
  truths:
    - "Gunicorn is bound to the college-assigned external port (not port 5000)"
    - "curl http://localhost:XXXX/health from inside the VM returns HTTP 200"
    - "curl http://200.69.13.70:XXXX/health from outside the VM returns HTTP 200 (direct, no tunnel)"
  artifacts:
    - path: "/etc/systemd/system/context-of-code.service"
      provides: "Updated ExecStart with --bind 0.0.0.0:XXXX (assigned port)"
      contains: "--bind 0.0.0.0:<ASSIGNED_PORT>"
  key_links:
    - from: "systemd unit ExecStart"
      to: "college gateway"
      via: "Gunicorn bind port matches gateway-forwarded port"
      pattern: "--bind 0.0.0.0:<ASSIGNED_PORT>"
---

<objective>
Rebind Gunicorn from port 5000 to the college-assigned external port so the aggregator is reachable from outside the college network.

Purpose: Port 5000 is not externally accessible — the college gateway only forwards the assigned port. This plan makes the aggregator reachable from agent machines.
Output: Gunicorn running on the assigned port; internal health check passing; direct external health check passing.
</objective>

<execution_context>
@/Users/tjcla/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjcla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zTjLocalFiles/IseVm/ssh-tunnel-notes.md
@zTjLocalFiles/IseVm/studentVMAccessGuide.md
@.planning/phases/05-agent-cutover-and-end-to-end-verification/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rebind Gunicorn to the assigned port in the systemd unit file</name>
  <files>/etc/systemd/system/context-of-code.service</files>
  <action>
    Prerequisite: ISE port assignment email received — do not start without the assigned port number.

    SSH into the VM:
      ssh -i /Users/tjcla/.ssh/tj_vm_key student@200.69.13.70 -p 2214

    Edit the systemd unit file:
      sudo nano /etc/systemd/system/context-of-code.service

    In the ExecStart block, change:
      --bind 0.0.0.0:5000 \
    to (XXXX = assigned port number from ISE email):
      --bind 0.0.0.0:XXXX \

    Save and exit nano: Ctrl+O, Enter, Ctrl+X.

    Reload and restart (daemon-reload is mandatory — without it systemd uses the cached old unit):
      sudo systemctl daemon-reload
      sudo systemctl restart context-of-code
      sudo systemctl status context-of-code --no-pager

    Verify health on new port from inside the VM:
      curl http://localhost:XXXX/health
    Expected: {"status": "ok", "ts": "2026-..."} HTTP 200

    Confirm port 5000 no longer responds (rebind confirmed):
      curl http://localhost:5000/health
    Expected: Connection refused.
  </action>
  <verify>
    sudo systemctl status context-of-code --no-pager
    curl http://localhost:XXXX/health
    Both must succeed. curl http://localhost:5000/health must fail.
  </verify>
  <done>
    systemctl status shows Active: active (running). curl http://localhost:XXXX/health returns {"status": "ok", ...}. curl http://localhost:5000/health is refused.
  </done>
  <what-built>
    Nothing is built autonomously — these are manual steps for Timothy to run on the VM.
  </what-built>
  <resume-signal>Paste the output of `systemctl status context-of-code --no-pager` and the curl response from `curl http://localhost:XXXX/health`. Type "done" when complete.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify direct external reachability (no SSH tunnel)</name>
  <files>N/A — read-only verification, no files modified</files>
  <action>
    From your laptop (NOT inside the VM, no SSH tunnel):
      curl -s -w "\nHTTP %{http_code}\n" http://200.69.13.70:XXXX/health

    Replace XXXX with the assigned port number.

    If HTTP 200: gateway is active — agents will reach the VM directly after .env update.
    If timeout or connection refused: gateway rule not yet active.
      - Contact ISE admin to confirm the gateway forwarding rule for XXXX is applied.
      - Do not proceed with agent cutover (Plan 02) until this returns 200.
      - Note the result as "gateway-pending" and continue to Plan 02 pre-cutover checks via SSH tunnel.
  </action>
  <verify>
    curl -s -w "\nHTTP %{http_code}\n" http://200.69.13.70:XXXX/health
    Expected: HTTP 200 with {"status": "ok", ...} body. OR documented gateway-pending with ISE admin contacted.
  </verify>
  <done>
    Direct external curl returns HTTP 200 (gateway active), OR result is documented as gateway-pending and ISE admin has been contacted to activate the rule.
  </done>
  <resume-signal>Paste the curl output (response body + HTTP status code). If the gateway is not yet active, type "gateway-pending" and we will proceed to Plan 02 pre-cutover checks via tunnel only.</resume-signal>
</task>

</tasks>

<verification>
Plan 01 is complete when:
1. `systemctl status context-of-code` shows `Active: active (running)` after the rebind
2. `curl http://localhost:XXXX/health` from inside the VM returns the JSON health response
3. `curl http://200.69.13.70:XXXX/health` from outside returns HTTP 200 (or gateway-pending is confirmed and ISE admin has been contacted)
</verification>

<success_criteria>
The college-assigned port is bound at the Gunicorn level and the systemd service is running on it. External direct access has been tested and the result (success or gateway-pending) is known and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-cutover-and-end-to-end-verification/05-01-SUMMARY.md` with:
- The assigned port number (fill in XXXX)
- Whether direct external access succeeded or is gateway-pending
- Output of `systemctl status context-of-code --no-pager`
- The curl responses from both the internal and external health checks
</output>

---
phase: 05-agent-cutover-and-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .env  # on each agent machine (not repo-tracked, one change per laptop)
autonomous: false
requirements:
  - CUTOVER-01
  - CUTOVER-02

must_haves:
  truths:
    - "Pre-cutover SSH tunnel test: /health returns 200 and /api/ingest test POST returns 200"
    - "AGGREGATOR_API_URL in each agent machine's .env points to http://200.69.13.70:XXXX/api/ingest"
    - "One agent run produces a log line confirming 200 response (Uploaded N queued sample(s).)"
    - "A new row appears in the Supabase samples table with a timestamp matching the agent run"
  artifacts:
    - path: ".env (each agent machine)"
      provides: "Updated AGGREGATOR_API_URL pointing to VM"
      contains: "AGGREGATOR_API_URL=http://200.69.13.70:<ASSIGNED_PORT>/api/ingest"
  key_links:
    - from: "agent AGGREGATOR_API_URL in .env"
      to: "VM /api/ingest endpoint"
      via: "UploadQueue._send_payload() HTTP POST"
      pattern: "http://200.69.13.70:<ASSIGNED_PORT>/api/ingest"
    - from: "VM /api/ingest endpoint"
      to: "Supabase samples table"
      via: "SQLAlchemy insert in web_app/blueprints/api.py"
      pattern: "INSERT INTO samples"
---

<objective>
Pre-verify the VM endpoint via SSH tunnel, coordinate a same-day cutover with all team members, update every agent's .env, and confirm end-to-end data flow with a new Supabase row and presentation-ready evidence.

Purpose: This is the final step of the migration — agents stop posting to PythonAnywhere and start posting to the VM. End-to-end verification proves the migration is complete.
Output: All agents pointed at the VM; one confirmed agent run logged; new Supabase row visible; screenshot + terminal output captured for presentation.
</objective>

<execution_context>
@/Users/tjcla/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjcla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zTjLocalFiles/IseVm/ssh-tunnel-notes.md
@zTjLocalFiles/IseVm/studentVMAccessGuide.md
@.planning/phases/05-agent-cutover-and-end-to-end-verification/05-RESEARCH.md
@.planning/phases/05-agent-cutover-and-end-to-end-verification/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-cutover endpoint verification via SSH tunnel</name>
  <files>N/A — verification only, no files modified</files>
  <action>
    This is a safety check before any agent is switched. Complete before coordinating cutover with teammates.

    Terminal 1 — open SSH tunnel (replace XXXX with the actual assigned port):
      ssh -i /Users/tjcla/.ssh/tj_vm_key \
          -L XXXX:localhost:XXXX \
          student@200.69.13.70 -p 2214
    Keep Terminal 1 open throughout.

    Terminal 2 — health check via tunnel:
      curl -s -w "\nHTTP %{http_code}\n" http://localhost:XXXX/health
    Expected: {"status": "ok", "ts": "2026-..."} and HTTP 200

    Terminal 2 — test POST to /api/ingest (verify endpoint accepts valid payloads):
      curl -s -w "\nHTTP %{http_code}\n" \
          -X POST http://localhost:XXXX/api/ingest \
          -H "Content-Type: application/json" \
          -d '{
              "device_id": "647fb7f6-9988-4656-b3db-49f19e834f63",
              "sample_type": "desktop_network",
              "ts": "2026-02-26T12:00:00+00:00",
              "latency_ms": 10.0,
              "packet_loss_pct": 0.0,
              "down_mbps": 100.0,
              "up_mbps": 50.0,
              "test_method": "http_fallback",
              "ip": "127.0.0.1"
          }'
    Expected: {"status": "ok", "sample_type": "desktop_network"} and HTTP 200

    If /api/ingest returns HTTP 500 with a foreign key error: the device_id UUID is not in the devices table. Use a real device UUID from the Supabase devices table (browser dashboard) instead.

    If either check fails: do NOT proceed. Diagnose with: journalctl -u context-of-code -n 50 (on VM).
  </action>
  <verify>
    curl -s -w "\nHTTP %{http_code}\n" http://localhost:XXXX/health  →  HTTP 200
    curl -s -w "\nHTTP %{http_code}\n" -X POST http://localhost:XXXX/api/ingest ...  →  HTTP 200
  </verify>
  <done>
    Both /health and /api/ingest test POST return HTTP 200 via SSH tunnel. VM is confirmed ready.
  </done>
  <resume-signal>Paste the output of both curl commands (health check and test POST). Type "vm-ready" when both return 200.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Coordinate cutover and update AGGREGATOR_API_URL on all agent machines</name>
  <files>.env (repo root on each agent machine — not version-controlled)</files>
  <action>
    Pre-cutover check (Task 1) passed. Now perform the simultaneous cutover on all machines.

    Step 1 — Agree on a cutover day with all teammates. Send them this message (fill in XXXX):

      "Aggregator migration cutover today. In your context-of-code repo root, open .env and change:

        AGGREGATOR_API_URL=https://tazo.pythonanywhere.com/api/ingest

      to:

        AGGREGATOR_API_URL=http://200.69.13.70:XXXX/api/ingest

      After saving, fully stop and restart the agent (Ctrl+C, then re-run). The settings cache means a restart is required — editing .env while the agent runs has no effect."

    Step 2 — Update your own .env on this machine:
      Open .env in repo root.
      Change: AGGREGATOR_API_URL=https://tazo.pythonanywhere.com/api/ingest
      To:     AGGREGATOR_API_URL=http://200.69.13.70:XXXX/api/ingest
      Save.

    Step 3 — Fully restart the agent on this machine:
      Stop: Ctrl+C
      Start: python -m agent (or whichever command runs the agent)

    Confirm the agent log does NOT show the old PythonAnywhere URL in warnings.
    A successful run shows: "Uploaded 1 queued sample(s)."

    If "Aggregator unreachable" or "Aggregator HTTP error" appears:
    - Confirm XXXX in .env is the exact assigned port number
    - Confirm the agent was fully restarted after the .env edit (not just the file saved while running)
  </action>
  <verify>
    Agent log shows "Uploaded N queued sample(s)." (not "Aggregator unreachable").
    .env contains AGGREGATOR_API_URL=http://200.69.13.70:XXXX/api/ingest on this machine.
  </verify>
  <done>
    AGGREGATOR_API_URL updated on all agent machines. This machine's agent ran and logged "Uploaded N queued sample(s)." confirming HTTP 200 from the VM.
  </done>
  <resume-signal>Confirm when all team members have updated their .env and restarted their agents. Paste your own agent's log output showing the Uploaded line. Type "cutover-done" when complete.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification — confirm Supabase row and capture presentation evidence</name>
  <files>N/A — verification and screenshot capture only</files>
  <action>
    All agents have been cut over. Verify the complete data flow from agent to Supabase and capture presentation artifacts.

    Step 1 — Confirm agent run succeeded:
    The agent log from Task 2 should show:
      "Uploaded 1 queued sample(s)."
    If not: check agent_queue.jsonl — it should be empty or shrinking, not growing.

    Step 2 — Check Supabase for new rows:
    1. Open https://supabase.com, log in
    2. Table Editor → samples table
    3. Sort by ts (timestamp) descending — newest first
    4. Find a row with ts matching the timestamp of the agent run from Task 2

    Expected: A new row with device_id, sample_type, ts, latency_ms, packet_loss_pct, down_mbps, up_mbps populated with real agent values.

    Step 3 — Capture two screenshots for the college presentation:
    - Screenshot 1: Supabase dashboard showing the new row (include the ts column)
    - Screenshot 2: Terminal output showing "Uploaded N queued sample(s)."

    If no new row appears:
    - Check agent .env still has the VM URL (not PythonAnywhere) — look for URL in any warning logs
    - On VM: journalctl -u context-of-code -n 50 — look for ingest errors (FK violations, DB errors)
    - Confirm the agent process was fully restarted after the .env edit
  </action>
  <verify>
    New row visible in Supabase samples table with ts matching the agent run time.
    Two screenshots captured: Supabase row + terminal "Uploaded N queued sample(s)." log line.
  </verify>
  <done>
    New Supabase row confirmed with real metric values from the agent run. Two presentation screenshots saved. Migration from PythonAnywhere to VM is complete and verified.
  </done>
  <resume-signal>Confirm you can see the new Supabase row and have the two screenshots saved. Type "verified" when the end-to-end check is complete and evidence is captured.</resume-signal>
</task>

</tasks>

<verification>
Plan 02 is complete when:
1. SSH tunnel pre-check confirmed both /health (200) and /api/ingest test POST (200)
2. All team members' .env files updated and agent processes restarted
3. At least one agent run logged "Uploaded N queued sample(s)."
4. New row visible in Supabase samples table with timestamp matching the agent run
5. Screenshot of Supabase row + terminal output captured for presentation
</verification>

<success_criteria>
End-to-end verified: an agent on a team member's machine posts real network metrics to the VM, the VM ingests them, and they appear in Supabase. The migration from PythonAnywhere to the VM is complete. Presentation evidence (Supabase screenshot + agent log) is in hand.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-cutover-and-end-to-end-verification/05-02-SUMMARY.md` with:
- The assigned port number used
- Whether direct external access was confirmed (from Plan 01) or relied on tunnel
- Pre-cutover curl outputs (health + test POST)
- Agent log line confirming upload success
- Confirmation that a new Supabase row was visible
- Location of the presentation screenshots
- Note confirming PythonAnywhere is no longer in use
</output>
